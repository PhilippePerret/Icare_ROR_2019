<%
#
# Mail envoyé à l'ADMIN À L'EXÉCUTION de l'action watcher
#
# L'action-watcher est bindé à cette vue
#
self.subject = "#{user.name} a téléchargé ses commentaires"

icetape   = self.objet
icmodule  = icetape.ic_module
%>
<pre>
DÉSIGNATION
-----------
Module    : <%= icmodule.abs_module.name %> (#<%= icmodule.id %>)
Étape     : <%= icetape.numero %> <%= icetape.titre %> (#<%= icetape.id %>)
Documents : <%= icetape.ic_documents.collect{|icd| icd.original_name }.to_sentence %>
Date      : <%= human_date_for(Date.today, avec_heure: true) %>
-----------
</pre>
